True Mobile:
Footer / back-to-top button are aligned left instead of center
spacing is not sufficient for footer on mobile
bottom line of footer needs to be split on mobile

slideshow is still not displaying

dash form displays far too low on true mobile

also need to create the skills map. x
Need to remake the image: add descriptions to the skills map. x
