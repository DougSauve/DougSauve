import React from 'react';

export default () => (
  <textarea
    className = "dash__input dash__form__width"
    rows = {3}
    cols = {50}
    name = "content"
    placeholder = "blogity blog blog..."
  />
);
